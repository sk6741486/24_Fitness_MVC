@model IEnumerable<_24_Fitness_MVC.Models.Trainer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trainer_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trainer_Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trainer_Adress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Specialization)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Trainer_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trainer_Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trainer_Adress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialization)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
